const usersSchema = {
  name: String,
  email: String,
  password: String,
  kodeAkses: String,
  noTelepon: Number,
  confirm_password: String,
  pin_mBank: Number,
  saldo: Number,
};

module.exports = usersSchema;
