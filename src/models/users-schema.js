const usersSchema = {
  name: String,
  email: String,
  password: String,
  kodeAkses: String,
  noTelepon: Number,
  confirm_password: String,
  pin_mBank: Number,
  saldo: Number,
  jumlah_transaksi: Number,
};

module.exports = usersSchema;
